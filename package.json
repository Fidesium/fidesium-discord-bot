{
  "name": "fidesium-discord-bot",
  "version": "1.0.0",
  "description": "Fidesium Discord Bot",
  "main": "dist/app.js",
  "type": "module",
  "scripts": {
    "start": "node .",
    "start-dev": "node .",
    "build": "rm -rf dist && tsc",
    "prestart-dev": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord-interactions": "^3.4.0",
    "discord.js": "^14.13.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "typescript": "^5.2.2"
  }
}
